# ============================================================
#  JARVIS — Self-Evolving AI Assistant
#  Dependency File
#
#  Only external dependency: LLM API connection
#  (OpenAI-compatible endpoint or local LM Studio)
#
#  JARVIS auto-installs missing deps on first run via bootstrap.
#  Use: pip install -r requirements.txt
# ============================================================

# ── CORE: Always required ──
openai>=1.0.0
anthropic>=0.30.0
google-genai>=1.0.0
python-dotenv>=1.0.0
pyyaml>=6.0
requests>=2.31.0
rich>=13.0.0
colorama>=0.4.6
psutil>=5.9.0
loguru>=0.7.0

# ── VOICE: Speech-to-text & Text-to-speech ──
edge-tts>=6.0.0
sounddevice>=0.4.6
soundfile>=0.12.0
numpy>=1.24.0
scipy>=1.10.0
faster-whisper>=1.0.0
pvporcupine>=3.0.0
pyaudio>=0.2.14

# ── MEMORY: Vector storage & semantic search ──
chromadb>=0.4.0
sentence-transformers>=2.2.0

# ── VISION: Camera & object detection ──
opencv-python>=4.8.0
mediapipe>=0.10.0
ultralytics>=8.0.0
Pillow>=10.0.0

# ── FACE: Enhanced UI rendering ──
pygame>=2.5.0
